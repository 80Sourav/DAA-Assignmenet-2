class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
       int l=0;
       for(int i=n+m-1;i>=m;i--){
            if(nums1[i]==0){
                nums1[i]=nums2[l];
                l++;
            }
       } 
       Arrays.sort(nums1);

    }
}
